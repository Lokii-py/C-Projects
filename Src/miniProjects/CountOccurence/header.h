#ifndef HEADER_H
#define HEADER_H

//takes array of integers and its size
//returns the maximum element in the array
int maxElement(int elements[], int size);

//takes in the array of integers and its size and the maximum element in the array
//Display the occurence of each element in the array
void occurrences(int elements[], int size, int max);

#endif